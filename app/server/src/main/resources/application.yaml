spring:
  jpa:
    show-sql: true
    database: postgresql

  datasource:
    url: jdbc:postgresql://localhost:5432/renty
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}

  liquibase:
    change-log: migrations/master.xml

springdoc:
  swagger-ui:
    path: '/swagger-ui'